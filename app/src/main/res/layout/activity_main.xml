<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.mdepak.countdowntimerview.MainActivity">

    <com.mdepak.countdowntimerview.CountDownTimerView
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:id="@+id/countDown"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="start"
        android:id="@+id/startBtn"
        android:layout_alignTop="@+id/pauseBtn"
        android:layout_alignStart="@+id/stopBtn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="stop"
        android:id="@+id/stopBtn"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="49dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PAUSE"
        android:id="@+id/pauseBtn"
        android:layout_below="@+id/countDown"
        android:layout_alignEnd="@+id/countDown" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="resume"
        android:id="@+id/resumeBtn"
        android:layout_alignTop="@+id/stopBtn"
        android:layout_alignEnd="@+id/countDown" />

</RelativeLayout>
